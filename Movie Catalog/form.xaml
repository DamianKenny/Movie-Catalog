<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register" Height="400" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Text="Register" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,20" Grid.Row="0"/>

        <!-- First Name -->
        <TextBlock Text="First name" Margin="0,0,0,5" Grid.Row="1"/>
        <TextBox Name="txtFirstName" Grid.Row="2" Margin="0,0,0,10"/>

        <!-- Last Name -->
        <TextBlock Text="Last name" Margin="0,0,0,5" Grid.Row="3"/>
        <TextBox Name="txtLastName" Grid.Row="4" Margin="0,0,0,10"/>

        <!-- Email Address -->
        <TextBlock Text="Email address" Margin="0,0,0,5" Grid.Row="5"/>
        <TextBox Name="txtEmailAddress" Grid.Row="6" Margin="0,0,0,10"/>

        <!-- Password -->
        <TextBlock Text="Password" Margin="0,0,0,5" Grid.Row="7"/>
        <PasswordBox Name="txtPassword" Grid.Row="8" Margin="0,0,0,10" 
                     PasswordChar="*" PlaceholderText="Must be at least 8 characters"/>

        <!-- Register Button -->
        <Button Content="Register" Grid.Row="9" Margin="0,20,0,0" HorizontalAlignment="Center" Width="100"/>
    </Grid>
</Window>
